ktor {
  development = true

  deployment {
      port = 8080
  }

  application {
      modules = [ com.github.tophatcroat.ApplicationKt.module ]
  }

  server {
    isProd = false
  }

  database {
    url = "jdbc:postgresql://localhost:5431/simple-dev"
    username = "simple"
    password = "simple"
    maxPoolSize = 3
  }
}
